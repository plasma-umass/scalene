\* Bug configuration: iterate the live dict directly (pre-fix scalene_json.py:875).
\* Expected result: NoIterationFault is VIOLATED -- TLC produces a counterexample
\* trace where the handler appends a key mid-iteration, reproducing
\* "RuntimeError: dictionary changed size during iteration".
SPECIFICATION Spec
CONSTANTS
    Keys = {k1, k2, k3}
    UseSnapshot = FALSE
INVARIANT TypeOK
INVARIANT NoIterationFault
