\* Fix configuration: iterate over a snapshot (list(stats.combined_stacks.items())).
\* Expected result: all invariants HOLD (no iteration fault is reachable).
SPECIFICATION Spec
CONSTANTS
    Keys = {k1, k2, k3}
    UseSnapshot = TRUE
INVARIANT TypeOK
INVARIANT NoIterationFault
INVARIANT SnapshotSound
INVARIANT SnapshotComplete
